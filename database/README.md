## database

The database uses Mysql primarily. To set it up on your local machine:

```bash
cd database/
mysql -u root -p < db.sql > output.tab
```

## Setup with docker

To setup with docker, simpley run:

```bash

```
